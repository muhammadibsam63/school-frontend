import React, { useEffect, useState } from 'react'; const emptyForm = { name: '', email: '', age: '', course: '', }; function validate(form) { const errors = {}; if (!form.name.trim()) { errors.name = 'Name is required'; } else if (form.name.trim().length < 2) { errors.name = 'Name must be at least 2 characters'; } if (!form.email.trim()) { errors.email = 'Email is required'; } else if (!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(form.email.trim())) { errors.email = 'Enter a valid email address'; } if (form.age === '' || form.age === null) { errors.age = 'Age is required'; } else if ( !Number.isInteger(Number(form.age)) || Number(form.age) <= 0 || Number(form.age) >= 120 ) { errors.age = 'Age must be a whole number between 1 and 119'; } if (!form.course.trim()) { errors.course = 'Course is required'; } else if (form.course.trim().length < 2) { errors.course = 'Course must be at least 2 characters'; } return errors; } export default function StudentFormModal({ student, onClose, onSubmit, submitting, serverError, }) { const [form, setForm] = useState(emptyForm); const [errors, setErrors] = useState({}); const isEdit = Boolean(student); useEffect(() => { if (student) { setForm({ name: student.name ?? '', email: student.email ?? '', age: student.age ?? '', course: student.course ?? '', }); } else { setForm(emptyForm); } setErrors({}); }, [student]); const handleChange = (field) => (e) => { setForm((prev) => ({ ...prev, [field]: e.target.value, })); }; const handleSubmit = (e) => { e.preventDefault(); const validationErrors = validate(form); setErrors(validationErrors); if (Object.keys(validationErrors).length === 0) { onSubmit({ name: form.name.trim(), email: form.email.trim(), age: Number(form.age), course: form.course.trim(), }); } }; return ( <div className="modal-overlay" role="presentation" onMouseDown={onClose} > <div className="modal" role="dialog" aria-modal="true" aria-labelledby="student-modal-title" onMouseDown={(e) => e.stopPropagation()} > <div className="modal__header"> <h2 id="student-modal-title" className="modal__title"> {isEdit ? 'Edit Student' : 'Add Student'} </h2> <button type="button" className="modal__close" aria-label="Close dialog" onClick={onClose} > &times; </button> </div> <form onSubmit={handleSubmit} noValidate> <div className="modal__body"> {serverError && ( <div className="alert alert--error"> {serverError} </div> )} {/* Full Name */} <div className="field"> <label htmlFor="name" className="field__label"> Full Name </label> <input id="name" type="text" className={`field__input ${ errors.name ? 'field__input--error' : '' }`} value={form.name} onChange={handleChange('name')} placeholder="Enter your full name" autoFocus /> {errors.name && ( <p className="field__error">{errors.name}</p> )} </div> {/* Email */} <div className="field"> <label htmlFor="email" className="field__label"> Email Address </label> <input id="email" type="email" className={`field__input ${ errors.email ? 'field__input--error' : '' }`} value={form.email} onChange={handleChange('email')} placeholder="Enter your email address" /> {errors.email && ( <p className="field__error">{errors.email}</p> )} </div> {/* Age & Course */} <div className="field-row"> {/* Age */} <div className="field"> <label htmlFor="age" className="field__label"> Age </label> <input id="age" type="number" min="1" max="119" className={`field__input ${ errors.age ? 'field__input--error' : '' }`} value={form.age} onChange={handleChange('age')} placeholder="Enter your age" /> {errors.age && ( <p className="field__error">{errors.age}</p> )} </div> {/* Course */} <div className="field"> <label htmlFor="course" className="field__label"> Course </label> <input id="course" type="text" className={`field__input ${ errors.course ? 'field__input--error' : '' }`} value={form.course} onChange={handleChange('course')} placeholder="Enter your course name" /> {errors.course && ( <p className="field__error">{errors.course}</p> )} </div> </div> </div> <div className="modal__footer"> <button type="button" className="btn btn--ghost" onClick={onClose} disabled={submitting} > Cancel </button> <button type="submit" className="btn btn--primary" disabled={submitting} > {submitting ? 'Saving…' : isEdit ? 'Save Changes' : 'Add Student'} </button> </div> </form> </div> </div> ); }